# No mandatory third-party packages are required for forensic recovery.
# PyYAML is optional: it is only used for OCIO color-profile configuration
# inspection. The recovery engine works without it.
#
# Optional:
# PyYAML>=6.0
